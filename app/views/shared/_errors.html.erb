<% if target.errors.any? %>
  <div id="error_explanation">
      <h2><%= pluralize(target.errors.count, "error")%> found:</h2>
      <ul>
        <% target.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
  </div>
<% end %>
